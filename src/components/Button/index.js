import React from 'react';

export default class Button extends React.Component {
    constructor() { }
    render() {
        return (
            <button>Hello</button>
        );
    }
}

export default Button;